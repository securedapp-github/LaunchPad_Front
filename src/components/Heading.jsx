import React from "react";

const Heading = ({ content }) => {
  return (
    <div className="text-white text-[36px] pr-[50px] font-bold font-sans leading-[110%] ">
      {content}
    </div>
  );
};

export default Heading;
